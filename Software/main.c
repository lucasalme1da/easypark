#include "main.h"

#include "string.h"
int main(void) {
  // le_arquivos(&d, &v);
  // le_input(&d);
  // calcula_vaga(d, &v);
  // verifica_vaga(&v);
  // exibe_info("jarrada");
  // comunicacao("dados");
  return (0);
}