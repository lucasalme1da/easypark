#include "main.h"

int main(void) {
  Destino d;
  Vaga v;

  // le_arquivos(&d, &v);
  // le_input(&d);
  calcula_vaga(d, &v);
  // verifica_vaga(&v);
  // exibe_info("jarrada");
  // comunicacao("dados");

  system("pause");
  return (0);
}